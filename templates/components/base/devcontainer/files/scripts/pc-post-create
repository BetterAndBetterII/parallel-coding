#!/usr/bin/env bash
set -euo pipefail

DIR="/workspaces/workspace/.devcontainer/scripts/post-create.d"
if [ ! -d "$DIR" ]; then
  exit 0
fi

shopt -s nullglob
for f in "$DIR"/*.sh; do
  echo "[pc] postCreate: $f"
  bash "$f"
done

